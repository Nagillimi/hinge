from sensor_collection import SensorCollection

class Kinematics:
    def waitForMotion(self, collection: SensorCollection) -> bool:
        True
    
    def waitForStillness(self, collection: SensorCollection) -> bool:
        True

        