from sensor_collection import SensorCollection

class Kinematics:
    def isMoving(self, collection: SensorCollection) -> bool:
        True
    
    def isStill(self, collection: SensorCollection) -> bool:
        True

        